# legacy/

Orphaned assets rescued from the untracked `deepgis_xr/apps/web/static/web/` tree
when it was deleted in refactor/tier-d0-static-tree-consolidation.

At the time of removal none of these files were referenced from any template,
JS, or Python code in the repo. They are preserved here only so we can
`git rm` them cleanly in a follow-up once confirmed dead.

- mask2former-corrector.css
- mask2former-corrector.js
- responsive.css
