/**
 * Tile catalog control bar.
 *
 * Phase 2: empty placeholder.
 * Phase 4 will add: comparison-mode selector (single / swipe / diff).
 * Phase 5 will add: "filter to viewport" toggle.
 *
 * Kept as a separate module so adding controls later doesn't require
 * touching the panel mount logic.
 */
export function renderControlBar(container, _opts) {
  if (!container) return;
  container.innerHTML = '';
}
